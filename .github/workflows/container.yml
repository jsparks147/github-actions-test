name: Container
on: push  

jobs:
  node-docker:
    runs-on: ubuntu-18.04
    container: 
      image: node:13.5.0-alpine3.10
    steps: 
      - name: Log node version in container
        run: |
          node -v
          cat /etc/os-release
